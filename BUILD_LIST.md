# Build List / Roadmap

Planned features and improvements.

## Backlog

- **Drag-and-drop column reorder** — Implemented. Mapped columns can be dragged to reorder; order controls display in reviewer layout.
- **Delete connection** — Implemented. Connections page has a Delete button with confirmation.
- **Tab/section assignment** — Allow assigning fields to specific tabs or sections when using tabbed/stacked layout (schema supports it; builder currently uses single section).
